spring:
  profiles:
    active: ${env:dev}
  application:
    serviceId : sequence-service
    name: sequence-service
  cloud:
    config:
      discovery:
            enabled: true
            service-id: config-service
      fail-fast: true
server:
  port: 9000

eureka:
  client:
    service-url:
            defaultZone: ${eureka.server:http://192.168.38.131:8000/eureka}