spring:
  profiles:
    active: ${env:dev}
  application:
    serviceId : sequence-service
    name: sequence-service
  cloud:
    config:
      discovery:
            enabled: true
            service-id: config-service
      fail-fast: true
server:
  port: 9000

eureka:
  client:
    service-url:
            defaultZone: ${eureka.server:http://10.10.90.24:8000/eureka}